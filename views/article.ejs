<%- include('partials/header') %>
<%- include('partials/homenavbar') %>
<body>
  <div class="container"><hr>
    <div class="card">
      <div class="card-header">
          <h2><%= article.title %></h2>
      </div>
      <div class="card-body">
        <%- article.body %> <!--Ao invés de utilizar <%= %> como uma variável, o sinal de '-' faz com que o conteúdo seja exibido com o HTML formatado/ativado-->
      </div>
    </div><br>
  </div>
</body>
<%- include('partials/footer') %>